import React from 'react';
import Calendar from 'react-calendar';

const CalendarView = () => {
  return (
    <div>
      <h2>Calendar View</h2>
      <Calendar />
    </div>
  );
};

export default CalendarView;
